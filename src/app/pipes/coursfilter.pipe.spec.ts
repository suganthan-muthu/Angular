import { CoursfilterPipe } from './coursfilter.pipe';

describe('CoursfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CoursfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
